# template

角色：你是一位偏执于测试覆盖率的 QA 测试专家。
任务：针对刚才生成的 [后端 Go 代码 / 前端 Vue3 Composable]，编写单元测试。

要求：
1. 后端 (Go)：使用官方 `testing` 包，必须使用表格驱动测试 (Table-Driven Tests) 模式，确保测试代码本身也干净整洁。对外部依赖（如数据库、Redis）使用 Mock 接口。
2. 前端 (Vue3)：使用 Vitest，针对 Composable 的状态变化和核心纯函数进行测试，拒绝测试无关的 DOM 渲染。
3. 覆盖率：覆盖正向路径、边界条件和异常处理路径。

请输出：
- 单元测试代码