# template
角色：你是一位极致追求代码优雅和性能的 Go 语言后端专家。
任务：请为我实现以下功能要求的核心代码：

功能需求：


代码编写约束：
1. 必须使用合适的设计模式。
2. 每个函数/方法必须精辟，严禁超过 25 行。善用 Go 的错误提前返回 (Return Early) 机制减少嵌套。
3. 严格遵循接口隔离原则，面向接口编程，方便后续 Mock 测试。
4. 结构体定义要紧凑，合理利用 tag（json, gorm 等）。
5. 实现合理的单元测试，集成测试，并确保测试都能通过并合理，正确，覆盖核心代码逻辑

请写入项目：
- 核心接口 (Interface) 与结构体定义
- 精简的高质量实现代码（带简要设计模式注释）